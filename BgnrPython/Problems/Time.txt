একদম ঠিক ভাই 😍
তুমি শেখাটা খুব গোছানোভাবে করছো — দারুন!
এখানে আমি তোমার জন্য **Python-এর `datetime` ও `time` মডিউলের সব গুরুত্বপূর্ণ built-in method ও concept গুলো সংক্ষিপ্ত সংজ্ঞাসহ সাজিয়ে দিলাম**, যেন তুমি পরে সহজে নোট রাখতে পারো 📒👇

---

# 🕒 **Python Time & Date — Full Summary Note**
================================================
--
## 🧩 **১. import datetime**

👉 সময় ও তারিখ (date, time) ব্যবস্থাপনার জন্য ব্যবহার হয়।

---

### 🕓 **২. datetime.datetime.now()**

বর্তমান সময় ও তারিখ দেয়।

```python
datetime.datetime.now()
```

🟢 উদাহরণ: `2025-10-18 14:37:22`

---

### 📅 **৩. datetime.date.today()**

শুধু আজকের তারিখ দেয়।

```python
datetime.date.today()
```

🟢 উদাহরণ: `2025-10-18`
---
### ⏱️ **৪. datetime.time()**

নির্দিষ্ট সময় তৈরি করে।

```python
datetime.time(10, 30, 45)
```

🟢 Output: `10:30:45`

---

### 📆 **৫. datetime.date()**

নির্দিষ্ট তারিখ তৈরি করে।

```python
datetime.date(2025, 12, 25)
```

🟢 Output: `2025-12-25`

---

### 🔁 **৬. datetime.timedelta()**

সময় বা দিনের ব্যবধান নির্ধারণ করে (যোগ/বিয়োগ করতে)।

```python
today + datetime.timedelta(days=7)
```

🟢 Output: আজ থেকে ৭ দিন পরের তারিখ।

---

### 🔄 **৭. datetime.strptime()**

String → Datetime রূপান্তর করে।

```python
datetime.strptime("2025-10-18", "%Y-%m-%d")
```

---

### 🧾 **৮. datetime.strftime()**

Datetime → String ফরম্যাটে রূপান্তর করে।

```python
now.strftime("%d-%b-%Y %I:%M %p")
```

🟢 Output: `18-Oct-2025 03:45 PM`

---

### 🌍 **৯. datetime.timezone / timedelta**

টাইমজোন হিসাব করতে ব্যবহৃত হয়।

```python
utc_time = datetime.now(timezone.utc)
bd_time = utc_time + timedelta(hours=6)
```

---

### 🔢 **১০. datetime attributes**

Datetime অবজেক্ট থেকে বছর, মাস, দিন, সময় বের করে।

```python
now.year, now.month, now.day
```

---

### 📆 **১১. datetime.date.replace()**

তারিখ বা সময়ের নির্দিষ্ট অংশ পরিবর্তন করে।

```python
today.replace(year=2026)
```

---

# ⏰ **time মডিউলের প্রধান মেথডগুলো**

---

### 🕐 **১. time.time()**

Epoch (1970 থেকে শুরু) থেকে কত সেকেন্ড কেটেছে তা দেয়।

```python
time.time()
```

---

### 💤 **২. time.sleep(seconds)**

প্রোগ্রাম নির্দিষ্ট সময়ের জন্য থামিয়ে রাখে (delay)।

```python
time.sleep(3)
```

---

### 🧮 **৩. time.ctime()**

টাইমস্ট্যাম্পকে পাঠযোগ্য ফরম্যাটে রূপান্তর করে।

```python
time.ctime(time.time())
```

🟢 Output: `Sat Oct 18 20:44:21 2025`

---

### 🕓 **৪. time.localtime()**

বর্তমান সময়কে struct_time অবজেক্টে দেয় (ডিটেইল সহ)।

```python
time.localtime()
```

---

### 📜 **৫. time.strftime()**

সময়কে নির্দিষ্ট ফরম্যাটে রূপান্তর করে (datetime-এর মতোই)।

```python
time.strftime("%H:%M:%S")
```

---

### 🔁 **৬. time.gmtime()**

UTC সময়ের struct_time দেয়।

```python
time.gmtime()
```

---

### ⏱️ **৭. time.perf_counter()**

নির্ভুল সময় মাপার জন্য (performance measurement)।

```python
start = time.perf_counter()
# কাজ
end = time.perf_counter()
```

---

# 🧠 **সবচেয়ে ব্যবহারযোগ্য ফরম্যাট কোডসমূহ**

| Code | অর্থ             | উদাহরণ   |
| ---- | ---------------- | -------- |
| `%Y` | বছর              | 2025     |
| `%m` | মাস (সংখ্যা)       | 10       |
| `%B` | মাসের নাম        | October  |
| `%d` | তারিখ            | 18       |
| `%A` | দিনের নাম        | Saturday |
| `%H` | ঘণ্টা (24 ঘন্টা)    | 14       |
| `%I` | ঘণ্টা (12 ঘন্টা)    | 02       |
| `%M` | মিনিট            | 30       |
| `%S` | সেকেন্ড           | 45       |
| `%p` | AM/PM            | PM       |

---

## 🧾 **সারসংক্ষেপ (Short Revision Table)**

| কাজ                  | মেথড             |
| -------------------- | ---------------- |
| বর্তমান সময়          | `datetime.now()` |
| আজকের তারিখ          | `date.today()`   |
| নির্দিষ্ট তারিখ তৈরি | `date()`         |
| নির্দিষ্ট সময় তৈরি   | `time()`         |
| সময় যোগ/বিয়োগ        | `timedelta()`    |
| স্ট্রিং থেকে সময়     | `strptime()`     |
| সময় থেকে স্ট্রিং     | `strftime()`     |
| প্রোগ্রাম থামানো     | `time.sleep()`   |
| টাইমস্ট্যাম্প দেখা   | `time.time()`    |
| মানবপাঠ্য সময়        | `time.ctime()`   |

---

তুমি চাইলে আমি এটা সুন্দরভাবে 📄 **PDF নোট ফরম্যাটে বানিয়ে দিতে পারি**
(বাংলায় সাজানো + Example সহ)
চাও কি আমি করে দিই ভাই?
